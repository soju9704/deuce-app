<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Deuce — 테니스 코치 매칭</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:wght@300;400;500;600&family=Noto+Sans+KR:wght@300;400;500;600&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{
  --green:#1a6b4a;--gl:#e8f5ee;--gm:#2d9062;
  --sand:#f5f0e8;--sand2:#ede7d8;
  --text:#1a1a1a;--text2:#555;--text3:#888;
  --white:#fff;--border:#e0d8cc;
  --r:16px;--rs:10px;
  --shadow:0 2px 16px rgba(0,0,0,.07);
}
body{font-family:'DM Sans','Noto Sans KR',sans-serif;background:var(--sand);color:var(--text);min-height:100vh;font-size:14px}

/* HEADER */
.header{background:var(--white);border-bottom:1px solid var(--border);padding:0 16px;height:54px;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:100;box-shadow:0 1px 8px rgba(0,0,0,.04)}
.logo{display:flex;align-items:center;gap:8px}
.logo-icon{width:32px;height:32px;background:var(--green);border-radius:9px;display:flex;align-items:center;justify-content:center;font-size:16px;box-shadow:0 2px 8px rgba(26,107,74,.3)}
.logo-text{font-family:'DM Serif Display',serif;font-size:20px;color:var(--green)}
.nav{display:flex;gap:4px}
.nav-btn{padding:6px 14px;border-radius:20px;border:none;font-family:inherit;font-size:12px;cursor:pointer;transition:all .2s;background:transparent;color:var(--text2);font-weight:500}
.nav-btn.active{background:var(--green);color:#fff;box-shadow:0 2px 8px rgba(26,107,74,.3)}
.lang-row{display:flex;gap:3px}
.lang-btn{padding:4px 9px;border-radius:6px;border:1px solid var(--border);font-size:11px;cursor:pointer;background:transparent;color:var(--text3);font-family:inherit;font-weight:500}
.lang-btn.active{background:var(--gl);color:var(--green);border-color:var(--green)}

/* AD */
.ad-zone{margin:12px 14px 0;position:relative}
.ad-label{position:absolute;top:8px;right:10px;background:rgba(0,0,0,.3);color:rgba(255,255,255,.9);font-size:9px;padding:2px 6px;border-radius:6px;z-index:2;letter-spacing:.03em}
.banner-slider{border-radius:var(--r);overflow:hidden;height:118px;box-shadow:var(--shadow)}
.banner-track{display:flex;height:100%;transition:transform .5s cubic-bezier(.4,0,.2,1)}
.banner-slide{min-width:100%;height:100%;position:relative;display:flex;align-items:center;padding:20px 22px;cursor:pointer}
.bs-1{background:linear-gradient(120deg,#0d3d6e,#1565a8)}
.bs-2{background:linear-gradient(120deg,#6b2200,#b03c00)}
.bs-3{background:linear-gradient(120deg,#163d20,#237a3c)}
.bs-4{background:linear-gradient(120deg,#3a0a6b,#7225b0)}
.bd{position:absolute;right:-20px;top:-20px;width:140px;height:140px;border-radius:50%;opacity:.1;background:#fff}
.bd2{position:absolute;right:50px;bottom:-40px;width:90px;height:90px;border-radius:50%;opacity:.07;background:#fff}
.b-icon{font-size:38px;margin-right:14px;flex-shrink:0;position:relative;z-index:1;filter:drop-shadow(0 2px 4px rgba(0,0,0,.2))}
.b-text{position:relative;z-index:1}
.b-tag{display:inline-block;background:rgba(255,255,255,.18);color:#fff;font-size:9px;padding:2px 8px;border-radius:10px;margin-bottom:5px;letter-spacing:.05em;font-weight:600}
.b-title{font-size:15px;font-weight:700;color:#fff;line-height:1.3;margin-bottom:4px}
.b-sub{font-size:11px;color:rgba(255,255,255,.78)}
.b-cta{display:inline-block;margin-top:7px;background:#fff;color:var(--text);font-size:10px;font-weight:700;padding:4px 12px;border-radius:10px;letter-spacing:.02em}
.banner-dots{display:flex;justify-content:center;gap:5px;margin-top:8px}
.banner-dot{width:5px;height:5px;border-radius:50%;background:var(--border);transition:all .3s;cursor:pointer}
.banner-dot.on{width:16px;border-radius:3px;background:var(--green)}

/* SEARCH */
.search-wrap{background:var(--white);margin:12px 14px 0;border-radius:var(--r);padding:16px;box-shadow:var(--shadow)}
.search-title{font-size:11px;font-weight:700;color:var(--text3);letter-spacing:.06em;text-transform:uppercase;margin-bottom:12px}
.region-row{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px}
.fi label{display:block;font-size:10px;color:var(--text3);margin-bottom:4px;font-weight:600;text-transform:uppercase;letter-spacing:.04em}
.fi select{width:100%;padding:9px 10px;border:1px solid var(--border);border-radius:var(--rs);font-family:inherit;font-size:13px;background:var(--sand);color:var(--text);outline:none;transition:border-color .15s}
.fi select:focus{border-color:var(--green);background:var(--white)}
.fi select:disabled{opacity:.4}
.chip-label{font-size:10px;color:var(--text3);font-weight:700;text-transform:uppercase;letter-spacing:.04em;margin-bottom:6px}
.chip-row{display:flex;gap:5px;flex-wrap:wrap;margin-bottom:12px}
.chip{padding:6px 12px;border-radius:20px;border:1px solid var(--border);font-size:12px;cursor:pointer;background:var(--white);color:var(--text2);transition:all .15s;font-family:inherit;font-weight:500}
.chip.on{background:var(--green);color:#fff;border-color:var(--green);box-shadow:0 2px 6px rgba(26,107,74,.25)}
.btn-search{width:100%;padding:12px;background:var(--green);color:#fff;border:none;border-radius:var(--rs);font-family:inherit;font-size:14px;font-weight:600;cursor:pointer;margin-top:4px;box-shadow:0 2px 10px rgba(26,107,74,.3);transition:opacity .15s}
.btn-search:hover{opacity:.88}

/* SECTION */
.section-label{padding:0 14px;margin:16px 0 10px;display:flex;justify-content:space-between;align-items:center}
.section-label h3{font-family:'DM Serif Display',serif;font-size:20px}
.section-label span{font-size:12px;color:var(--green);font-weight:600}

/* CARDS */
.coaches-list{padding:0 14px;display:flex;flex-direction:column;gap:12px;margin-bottom:90px}
.coach-card{background:var(--white);border-radius:var(--r);overflow:hidden;border:1px solid var(--border);cursor:pointer;transition:transform .15s,box-shadow .15s;box-shadow:var(--shadow)}
.coach-card:hover{transform:translateY(-2px);box-shadow:0 8px 28px rgba(0,0,0,.1)}
.card-top{display:flex;gap:12px;padding:16px}
.avatar{width:60px;height:60px;border-radius:13px;background:var(--gl);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:22px;font-weight:700;color:var(--green);overflow:hidden;border:2px solid rgba(26,107,74,.1)}
.avatar img{width:100%;height:100%;object-fit:cover}
.coach-info{flex:1;min-width:0}
.coach-name-row{display:flex;align-items:center;gap:6px;margin-bottom:3px;flex-wrap:wrap}
.coach-name{font-size:16px;font-weight:700}
.verified-badge{background:var(--green);color:#fff;font-size:9px;padding:2px 7px;border-radius:10px;font-weight:600;letter-spacing:.02em}
.coach-loc{font-size:11px;color:var(--text3);margin-bottom:4px;font-weight:500}
.rating-row{display:flex;align-items:center;gap:4px;font-size:12px;color:var(--text2)}
.stars{color:#f0b429;font-size:12px;letter-spacing:-1px}
.lesson-pills{display:flex;gap:5px;margin-top:6px;flex-wrap:wrap}
.lp{padding:3px 9px;border-radius:8px;font-size:10px;font-weight:600}
.lp-h{background:#eef1ff;color:#3b5bdb}
.lp-f{background:#fff4e6;color:#e67700}
.card-bottom{padding:10px 16px;border-top:1px solid var(--border);background:var(--sand)}
.card-tags{display:flex;gap:4px;flex-wrap:wrap;margin-bottom:7px}
.spec-tag{padding:3px 9px;background:var(--gl);color:var(--green);border-radius:8px;font-size:10px;font-weight:600}
.skill-tag{padding:3px 9px;background:#f0f4ff;color:#4263eb;border-radius:8px;font-size:10px;font-weight:600}
.price-row{display:flex;gap:12px;flex-wrap:wrap}
.price-item{display:flex;align-items:center;gap:5px;font-size:12px}
.pdot{width:7px;height:7px;border-radius:50%;flex-shrink:0}
.pdot-h{background:#3b5bdb}.pdot-f{background:#e67700}
.pval{font-weight:700;color:var(--green);font-size:13px}
.plbl{color:var(--text3);font-size:10px}

/* BOTTOM NAV */
.bottom-nav{position:fixed;bottom:0;left:0;right:0;background:var(--white);border-top:1px solid var(--border);display:flex;padding:8px 0 14px;box-shadow:0 -2px 12px rgba(0,0,0,.05)}
.bnav-btn{flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;border:none;background:transparent;cursor:pointer;font-family:inherit;font-size:10px;color:var(--text3);padding:4px 0;font-weight:500}
.bnav-btn.active{color:var(--green)}
.bnav-icon{font-size:20px}

/* MODAL */
.modal-bg{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:200;align-items:flex-end}
.modal-bg.show{display:flex}
.modal{background:var(--white);border-radius:24px 24px 0 0;width:100%;max-height:90vh;overflow-y:auto;padding:20px;position:relative}
.modal-handle{width:44px;height:4px;background:var(--border);border-radius:2px;margin:0 auto 18px}
.modal-avatar{width:80px;height:80px;border-radius:16px;background:var(--gl);display:flex;align-items:center;justify-content:center;font-size:30px;font-weight:700;color:var(--green);margin-bottom:12px;overflow:hidden;border:2px solid rgba(26,107,74,.1)}
.modal-avatar img{width:100%;height:100%;object-fit:cover}
.modal-name{font-family:'DM Serif Display',serif;font-size:24px;margin-bottom:3px}
.modal-loc{font-size:12px;color:var(--text3);margin-bottom:14px;font-weight:500}
.modal-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}
.mstat{background:var(--sand);border-radius:var(--rs);padding:11px;text-align:center}
.mstat .n{font-size:17px;font-weight:700;color:var(--green)}
.mstat .l{font-size:10px;color:var(--text3);margin-top:2px;font-weight:500}
.msec{margin-bottom:14px}
.msec h4{font-size:10px;font-weight:700;color:var(--text3);text-transform:uppercase;letter-spacing:.05em;margin-bottom:7px}
.msec p{font-size:13px;color:var(--text2);line-height:1.7}
.tag-row{display:flex;gap:5px;flex-wrap:wrap}
.divider{height:.5px;background:var(--border);margin:12px 0}
.close-btn{position:absolute;top:20px;right:20px;background:var(--sand);border:none;width:32px;height:32px;border-radius:50%;cursor:pointer;font-size:15px;display:flex;align-items:center;justify-content:center}
.btn-book{width:100%;padding:14px;background:var(--green);color:#fff;border:none;border-radius:12px;font-size:15px;font-weight:700;font-family:inherit;cursor:pointer;margin-top:10px;box-shadow:0 3px 12px rgba(26,107,74,.3)}
.mprice-box{background:var(--sand);border-radius:var(--rs);padding:13px;display:flex;flex-direction:column;gap:10px}
.mprice-row{display:flex;align-items:center;justify-content:space-between}
.mprice-lbl{display:flex;align-items:center;gap:7px;font-size:13px;color:var(--text2);font-weight:500}
.mprice-val{font-size:18px;font-weight:700;color:var(--green)}
.modal-video a{color:var(--green);font-size:13px;text-decoration:none;display:flex;align-items:center;gap:6px;padding:6px 0;border-bottom:1px solid var(--border);font-weight:500}
.modal-video a:last-child{border-bottom:none}

/* REGISTER — 스텝 방식 */
.reg-page{display:none;padding:14px;margin-bottom:90px}
.reg-page.show{display:block}

/* 진행 바 */
.progress-wrap{background:var(--white);border-radius:var(--r);padding:14px 16px;margin-bottom:12px;box-shadow:var(--shadow)}
.progress-steps{display:flex;justify-content:space-between;margin-bottom:10px}
.ps-item{display:flex;flex-direction:column;align-items:center;gap:3px;flex:1}
.ps-dot{width:24px;height:24px;border-radius:50%;background:var(--border);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:var(--text3);transition:all .3s}
.ps-dot.done{background:var(--green);color:#fff}
.ps-dot.active{background:var(--green);color:#fff;box-shadow:0 0 0 4px rgba(26,107,74,.2)}
.ps-label{font-size:9px;color:var(--text3);font-weight:600;text-align:center}
.progress-bar-track{height:4px;background:var(--border);border-radius:2px}
.progress-bar-fill{height:100%;background:var(--green);border-radius:2px;transition:width .4s}

.step-panel{display:none}
.step-panel.on{display:block}
.reg-section{background:var(--white);border-radius:var(--r);padding:16px;margin-bottom:10px;border:1px solid var(--border);box-shadow:var(--shadow)}
.reg-section h3{font-size:14px;font-weight:700;margin-bottom:4px;display:flex;align-items:center;gap:6px}
.sec-desc{font-size:11px;color:var(--text3);margin-bottom:14px;line-height:1.6}
.form-row{margin-bottom:12px}
.form-row label{display:block;font-size:10px;color:var(--text3);font-weight:700;margin-bottom:4px;text-transform:uppercase;letter-spacing:.04em}
.form-row input,.form-row select,.form-row textarea{width:100%;padding:10px 12px;border:1.5px solid var(--border);border-radius:var(--rs);font-family:inherit;font-size:13px;background:var(--sand);color:var(--text);outline:none;transition:all .15s}
.form-row input:focus,.form-row select:focus,.form-row textarea:focus{border-color:var(--green);background:var(--white);box-shadow:0 0 0 3px rgba(26,107,74,.08)}
.form-2col{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.form-hint{font-size:10px;color:var(--text3);margin-top:4px;line-height:1.5;display:block}
.form-example{background:var(--sand);border-radius:8px;padding:9px 11px;margin-top:6px;font-size:11px;color:var(--text2);line-height:1.8;border-left:3px solid var(--green)}
.form-example b{color:var(--green)}

.price-input-wrap{position:relative}
.price-input-wrap input{padding-right:32px}
.price-unit{position:absolute;right:12px;top:50%;transform:translateY(-50%);font-size:12px;color:var(--text3);pointer-events:none}
.price-display{font-size:11px;color:var(--green);margin-top:4px;font-weight:600;min-height:16px}

.lesson-opt{padding:12px 14px;border:1.5px solid var(--border);border-radius:var(--rs);cursor:pointer;margin-bottom:8px;transition:all .15s}
.lesson-opt.on{border-color:var(--green);background:var(--gl)}
.lo-t{font-size:13px;font-weight:700;margin-bottom:2px}
.lo-d{font-size:11px;color:var(--text3);line-height:1.5}
.lesson-price-input{margin-top:10px;display:none}
.lesson-opt.on .lesson-price-input{display:block}

.day-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;margin-bottom:12px}
.day-btn{padding:8px 2px;border-radius:8px;border:1.5px solid var(--border);font-size:12px;cursor:pointer;background:var(--white);color:var(--text2);text-align:center;font-family:inherit;transition:all .15s;font-weight:600}
.day-btn.on{background:var(--green);color:#fff;border-color:var(--green)}

.time-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:8px}
.time-btn{padding:9px 6px;border-radius:8px;border:1.5px solid var(--border);font-size:11px;cursor:pointer;background:var(--white);color:var(--text2);text-align:center;font-family:inherit;transition:all .15s;line-height:1.4}
.time-btn.on{background:var(--green);color:#fff;border-color:var(--green)}
.t-name{font-weight:700;display:block;margin-bottom:1px}
.t-range{font-size:10px;opacity:.8}

.media-tabs{display:flex;gap:4px;margin-bottom:10px}
.media-tab{padding:6px 12px;border-radius:20px;border:1px solid var(--border);font-size:12px;cursor:pointer;background:var(--white);color:var(--text2);font-family:inherit;font-weight:500}
.media-tab.on{background:var(--green);color:#fff;border-color:var(--green)}
.media-panel{display:none}.media-panel.on{display:block}
.upload-box{border:2px dashed var(--border);border-radius:var(--rs);padding:20px;text-align:center;cursor:pointer;color:var(--text3);font-size:13px;transition:all .15s;font-weight:500}
.upload-box:hover{border-color:var(--green);color:var(--green)}
.upload-box input{display:none}
.preview-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:8px}
.preview-item{aspect-ratio:1;border-radius:8px;overflow:hidden;position:relative}
.preview-item img{width:100%;height:100%;object-fit:cover}
.preview-remove{position:absolute;top:3px;right:3px;background:rgba(0,0,0,.55);color:#fff;border:none;border-radius:50%;width:20px;height:20px;font-size:11px;cursor:pointer}
.link-row{display:flex;gap:6px;margin-bottom:6px;align-items:center}
.link-row input{flex:1;padding:9px 10px;border:1.5px solid var(--border);border-radius:var(--rs);font-family:inherit;font-size:12px;background:var(--sand);outline:none;transition:all .15s}
.link-row input:focus{border-color:var(--green);background:var(--white)}
.link-add-btn{padding:9px 12px;background:var(--green);color:#fff;border:none;border-radius:var(--rs);font-size:12px;cursor:pointer;font-family:inherit;white-space:nowrap;font-weight:600}
.link-remove-btn{padding:7px 9px;background:transparent;border:1.5px solid var(--border);border-radius:var(--rs);font-size:12px;cursor:pointer;color:var(--text3)}

.step-nav{display:flex;gap:8px;margin-top:4px}
.btn-prev{flex:1;padding:13px;background:var(--white);border:1.5px solid var(--border);border-radius:12px;font-family:inherit;font-size:14px;font-weight:600;cursor:pointer;color:var(--text2)}
.btn-next{flex:2;padding:13px;background:var(--green);color:#fff;border:none;border-radius:12px;font-family:inherit;font-size:14px;font-weight:700;cursor:pointer;box-shadow:0 3px 12px rgba(26,107,74,.3);transition:opacity .15s}
.btn-next:hover{opacity:.88}
.btn-next:disabled{opacity:.5;cursor:not-allowed}

.intro-hint{background:var(--sand);border-radius:8px;padding:9px 11px;font-size:11px;color:var(--text2);line-height:1.7;margin-bottom:8px}

.loading{text-align:center;padding:40px;color:var(--text3);font-size:13px}
.no-results{text-align:center;padding:40px;color:var(--text3);font-size:13px}
.toast{display:none;position:fixed;top:64px;left:14px;right:14px;color:#fff;padding:13px 16px;border-radius:12px;z-index:300;font-size:13px;text-align:center;font-weight:600;box-shadow:0 4px 16px rgba(0,0,0,.15)}
.toast.show{display:block}
.toast.success{background:var(--green)}.toast.error{background:#c0392b}

/* 등록 완료 화면 */
.success-screen{text-align:center;padding:40px 20px}
.success-icon{font-size:64px;margin-bottom:16px}
.success-title{font-family:'DM Serif Display',serif;font-size:24px;margin-bottom:8px;color:var(--green)}
.success-desc{font-size:13px;color:var(--text2);line-height:1.7;margin-bottom:24px}
.btn-go-home{padding:13px 32px;background:var(--green);color:#fff;border:none;border-radius:12px;font-family:inherit;font-size:14px;font-weight:700;cursor:pointer;box-shadow:0 3px 12px rgba(26,107,74,.3)}
</style>
</head>
<body>

<div class="header">
  <div class="logo">
    <div class="logo-icon">🎾</div>
    <span class="logo-text">Deuce</span>
  </div>
  <div class="nav">
    <button class="nav-btn active" onclick="showPage('coaches');setNav(this)">찾기</button>
    <button class="nav-btn" onclick="showPage('register');setNav(this)">코치등록</button>
  </div>
  <div class="lang-row">
    <button class="lang-btn" onclick="setLang('en',this)">EN</button>
    <button class="lang-btn active" onclick="setLang('ko',this)">한글</button>
  </div>
</div>

<!-- 코치 찾기 -->
<div id="coaches-page">
  <div class="ad-zone">
    <span class="ad-label">AD</span>
    <div class="banner-slider">
      <div class="banner-track" id="bt">
        <div class="banner-slide bs-1"><div class="bd"></div><div class="bd2"></div><div class="b-icon">🎾</div><div class="b-text"><div class="b-tag">TENNIS SHOP</div><div class="b-title">스매시 스포츠<br>라켓 최대 30% 할인</div><div class="b-sub">Wilson · Babolat · Head 전 라인</div><div class="b-cta">지금 보기 →</div></div></div>
        <div class="banner-slide bs-2"><div class="bd"></div><div class="bd2"></div><div class="b-icon">🏟️</div><div class="b-text"><div class="b-tag">TENNIS COURT</div><div class="b-title">강남 프리미엄<br>테니스 클럽</div><div class="b-sub">실내 8면 · 주차 무료 · 샤워실 완비</div><div class="b-cta">코트 예약 →</div></div></div>
        <div class="banner-slide bs-3"><div class="bd"></div><div class="bd2"></div><div class="b-icon">👟</div><div class="b-text"><div class="b-tag">TENNIS WEAR</div><div class="b-title">나이키 테니스<br>신상 컬렉션 입고</div><div class="b-sub">코트화 · 의류 · 가방 전 품목</div><div class="b-cta">컬렉션 보기 →</div></div></div>
        <div class="banner-slide bs-4"><div class="bd"></div><div class="bd2"></div><div class="b-icon">🏆</div><div class="b-text"><div class="b-tag">ACADEMY</div><div class="b-title">서울 엘리트<br>테니스 아카데미</div><div class="b-sub">신규 회원 모집 · 체험 레슨 무료</div><div class="b-cta">신청하기 →</div></div></div>
      </div>
    </div>
    <div class="banner-dots">
      <div class="banner-dot on" onclick="goSlide(0)"></div>
      <div class="banner-dot" onclick="goSlide(1)"></div>
      <div class="banner-dot" onclick="goSlide(2)"></div>
      <div class="banner-dot" onclick="goSlide(3)"></div>
    </div>
  </div>

  <div class="search-wrap">
    <div class="search-title">코치 검색</div>
    <div class="region-row">
      <div class="fi"><label>광역시/도</label>
        <select id="f-region" onchange="onRegionChange()">
          <option value="">전체</option>
          <option>서울</option><option>경기도</option><option>인천</option><option>강원도</option><option>경상남도</option><option>경상북도</option><option>충청남도</option><option>충청북도</option><option>전라남도</option><option>전라북도</option><option>제주도</option>
        </select>
      </div>
      <div class="fi"><label>시/구</label>
        <select id="f-sub" onchange="filterCoaches()" disabled><option value="">전체</option></select>
      </div>
    </div>
    <div class="fi" style="margin-bottom:12px"><label>정렬</label>
      <select id="f-sort" onchange="filterCoaches()">
        <option value="rating">평점 높은 순</option><option value="price-low">요금 낮은 순</option><option value="price-high">요금 높은 순</option><option value="exp">경력 순</option>
      </select>
    </div>
    <div class="chip-label">수준</div>
    <div class="chip-row" id="lf">
      <div class="chip on" onclick="tc(this,'level','')">전체</div><div class="chip" onclick="tc(this,'level','초급')">초급</div><div class="chip" onclick="tc(this,'level','중급')">중급</div><div class="chip" onclick="tc(this,'level','고급')">고급</div><div class="chip" onclick="tc(this,'level','주니어')">주니어</div><div class="chip" onclick="tc(this,'level','시니어')">시니어</div>
    </div>
    <div class="chip-label">전문 스킬</div>
    <div class="chip-row" id="sf">
      <div class="chip on" onclick="tc(this,'skill','')">전체</div><div class="chip" onclick="tc(this,'skill','포핸드')">포핸드</div><div class="chip" onclick="tc(this,'skill','백핸드')">백핸드</div><div class="chip" onclick="tc(this,'skill','발리')">발리</div><div class="chip" onclick="tc(this,'skill','게임운영')">게임운영</div><div class="chip" onclick="tc(this,'skill','복식')">복식</div><div class="chip" onclick="tc(this,'skill','단식')">단식</div>
    </div>
    <div class="chip-label">레슨 종류</div>
    <div class="chip-row" id="lesf">
      <div class="chip on" onclick="tc(this,'lesson','')">전체</div><div class="chip" onclick="tc(this,'lesson','hitting')">🔵 히팅 레슨</div><div class="chip" onclick="tc(this,'lesson','feed')">🟠 볼피딩 레슨</div>
    </div>
    <button class="btn-search" onclick="filterCoaches()">🔍 코치 검색</button>
  </div>

  <div class="section-label">
    <h3>추천 코치</h3>
    <span id="cnt"></span>
  </div>
  <div class="coaches-list" id="coaches-list"><div class="loading">⏳ 코치 목록 불러오는 중...</div></div>
</div>

<!-- 코치 등록 — 단계별 폼 -->
<div id="register-page" class="reg-page">

  <!-- 진행 표시 -->
  <div class="progress-wrap">
    <div class="progress-steps">
      <div class="ps-item"><div class="ps-dot active" id="pd1">1</div><div class="ps-label">기본정보</div></div>
      <div class="ps-item"><div class="ps-dot" id="pd2">2</div><div class="ps-label">사진/영상</div></div>
      <div class="ps-item"><div class="ps-dot" id="pd3">3</div><div class="ps-label">경력</div></div>
      <div class="ps-item"><div class="ps-dot" id="pd4">4</div><div class="ps-label">전문분야</div></div>
      <div class="ps-item"><div class="ps-dot" id="pd5">5</div><div class="ps-label">레슨/시간</div></div>
    </div>
    <div class="progress-bar-track"><div class="progress-bar-fill" id="prog-fill" style="width:20%"></div></div>
  </div>

  <!-- STEP 1: 기본 정보 -->
  <div id="step1" class="step-panel on">
    <div class="reg-section">
      <h3>👤 기본 정보</h3>
      <div class="sec-desc">코치님의 기본 정보를 입력해주세요.</div>
      <div class="form-2col">
        <div class="form-row"><label>이름 *</label><input type="text" id="r-name" placeholder="홍길동"></div>
        <div class="form-row"><label>이메일 *</label><input type="email" id="r-email" placeholder="coach@example.com"></div>
      </div>
      <div class="form-row"><label>전화번호</label><input type="tel" id="r-phone" placeholder="010-1234-5678"></div>
      <div class="form-2col">
        <div class="form-row"><label>광역시/도 *</label>
          <select id="r-region" onchange="onRegRegionChange()">
            <option value="">선택</option>
            <option>서울</option><option>경기도</option><option>인천</option><option>강원도</option><option>경상남도</option><option>경상북도</option><option>충청남도</option><option>충청북도</option><option>전라남도</option><option>전라북도</option><option>제주도</option>
          </select>
        </div>
        <div class="form-row"><label>시/구 *</label>
          <select id="r-sub" disabled><option value="">광역시/도 먼저</option></select>
        </div>
      </div>
    </div>
    <div class="step-nav">
      <button class="btn-next" onclick="goStep(2)">다음 →</button>
    </div>
  </div>

  <!-- STEP 2: 사진/영상 -->
  <div id="step2" class="step-panel">
    <div class="reg-section">
      <h3>📸 사진 & 영상</h3>
      <div class="sec-desc">프로필 사진과 코칭 영상을 추가하면 더 많은 플레이어가 찾아요!</div>
      <div class="form-row">
        <label>프로필 사진</label>
        <div class="media-tabs">
          <button class="media-tab on" onclick="switchTab(this,'photo-url')">🔗 URL</button>
          <button class="media-tab" onclick="switchTab(this,'photo-file')">📁 파일</button>
        </div>
        <div id="photo-url" class="media-panel on">
          <input type="url" id="r-photo-url" placeholder="https://example.com/photo.jpg" oninput="previewPhotoUrl(this.value)">
          <div id="photo-url-prev" style="display:none;margin-top:8px"><img id="photo-url-img" src="" style="width:64px;height:64px;border-radius:12px;object-fit:cover;border:1.5px solid var(--border)"></div>
        </div>
        <div id="photo-file" class="media-panel">
          <div class="upload-box" onclick="document.getElementById('r-photo-file').click()">
            📷 클릭해서 사진 선택<br><span style="font-size:11px;margin-top:4px;display:block">JPG, PNG (최대 5MB)</span>
            <input type="file" id="r-photo-file" accept="image/*" onchange="previewPhotoFile(this)">
          </div>
          <div id="photo-file-prev" style="display:none;margin-top:8px;align-items:center;gap:8px">
            <img id="photo-file-img" src="" style="width:64px;height:64px;border-radius:12px;object-fit:cover;border:1.5px solid var(--border)">
            <button onclick="clearPhotoFile()" style="padding:4px 10px;border:1px solid var(--border);border-radius:8px;font-size:11px;cursor:pointer;background:transparent">삭제</button>
          </div>
        </div>
      </div>
      <div class="form-row">
        <label>코칭 영상 링크 (최대 3개)</label>
        <span class="form-hint">유튜브 또는 인스타그램 링크를 추가해주세요</span>
        <div id="video-links" style="margin-top:6px">
          <div class="link-row">
            <input type="url" placeholder="https://youtube.com/... 또는 https://instagram.com/reel/...">
            <button class="link-add-btn" onclick="addVideoLink()">+ 추가</button>
          </div>
        </div>
      </div>
    </div>
    <div class="step-nav">
      <button class="btn-prev" onclick="goStep(1)">← 이전</button>
      <button class="btn-next" onclick="goStep(3)">다음 →</button>
    </div>
  </div>

  <!-- STEP 3: 경력 -->
  <div id="step3" class="step-panel">
    <div class="reg-section">
      <h3>🏆 경력</h3>
      <div class="form-2col">
        <div class="form-row"><label>선수 경력 (년)</label><input type="number" id="r-pcareer" min="0" placeholder="0"></div>
        <div class="form-row"><label>코칭 경력 (년) *</label><input type="number" id="r-ccareer" min="0" placeholder="5"></div>
      </div>
      <div class="form-row">
        <label>선수 경력 상세</label>
        <div class="form-example"><b>✏️ 연도 — 소속/대회 — 성과 (최대 3개)</b><br>예) 2018 — 전국대학테니스선수권 — 준우승<br>예) 2015 — ITF 주니어 — 세계 랭킹 300위</div>
        <textarea id="r-pbg" rows="3" style="margin-top:8px" placeholder="2018 — 전국대학테니스선수권 — 준우승"></textarea>
      </div>
      <div class="form-row">
        <label>코칭 경력 상세</label>
        <div class="form-example"><b>✏️ 연도 — 소속/기관 — 역할 (최대 3개)</b><br>예) 2020~현재 — OO 테니스 아카데미 — 성인반 코치<br>예) 2018 — KTA 코치 자격증 1급 취득</div>
        <textarea id="r-cbg" rows="3" style="margin-top:8px" placeholder="2020~현재 — OO 테니스 아카데미 — 성인반 코치"></textarea>
      </div>
    </div>
    <div class="step-nav">
      <button class="btn-prev" onclick="goStep(2)">← 이전</button>
      <button class="btn-next" onclick="goStep(4)">다음 →</button>
    </div>
  </div>

  <!-- STEP 4: 전문분야 & 스킬 -->
  <div id="step4" class="step-panel">
    <div class="reg-section">
      <h3>⭐ 전문 분야</h3>
      <div class="chip-label" style="margin-bottom:7px">수준 (복수 선택)</div>
      <div class="chip-row" id="r-levels">
        <div class="chip" onclick="this.classList.toggle('on')">초급</div><div class="chip" onclick="this.classList.toggle('on')">중급</div><div class="chip" onclick="this.classList.toggle('on')">고급</div><div class="chip" onclick="this.classList.toggle('on')">주니어</div><div class="chip" onclick="this.classList.toggle('on')">시니어</div><div class="chip" onclick="this.classList.toggle('on')">그룹레슨</div>
      </div>
    </div>
    <div class="reg-section">
      <h3>🎯 전문 스킬</h3>
      <div class="chip-row" id="r-skills">
        <div class="chip" onclick="this.classList.toggle('on')">포핸드</div><div class="chip" onclick="this.classList.toggle('on')">백핸드</div><div class="chip" onclick="this.classList.toggle('on')">발리</div><div class="chip" onclick="this.classList.toggle('on')">게임운영</div><div class="chip" onclick="this.classList.toggle('on')">복식</div><div class="chip" onclick="this.classList.toggle('on')">단식</div>
      </div>
    </div>
    <div class="step-nav">
      <button class="btn-prev" onclick="goStep(3)">← 이전</button>
      <button class="btn-next" onclick="goStep(5)">다음 →</button>
    </div>
  </div>

  <!-- STEP 5: 레슨/시간/소개 + 제출 -->
  <div id="step5" class="step-panel">
    <div class="reg-section">
      <h3>🎾 레슨 종류 및 요금</h3>
      <div class="lesson-opt" id="opt-hitting" onclick="this.classList.toggle('on')">
        <div class="lo-t">🔵 히팅 레슨</div>
        <div class="lo-d">코치와 직접 랠리하며 실전 감각을 키우는 레슨</div>
        <div class="lesson-price-input" onclick="event.stopPropagation()">
          <label style="font-size:10px;color:var(--text3);display:block;margin-bottom:4px;font-weight:600">시간당 요금</label>
          <div class="price-input-wrap">
            <input type="text" id="r-hrate" placeholder="80,000" oninput="formatPrice(this,'r-hrate-disp')" inputmode="numeric">
            <span class="price-unit">원</span>
          </div>
          <div class="price-display" id="r-hrate-disp"></div>
        </div>
      </div>
      <div class="lesson-opt" id="opt-feed" onclick="this.classList.toggle('on')">
        <div class="lo-t">🟠 볼피딩 레슨</div>
        <div class="lo-d">코치가 공을 던져주며 자세와 폼을 교정하는 레슨</div>
        <div class="lesson-price-input" onclick="event.stopPropagation()">
          <label style="font-size:10px;color:var(--text3);display:block;margin-bottom:4px;font-weight:600">시간당 요금</label>
          <div class="price-input-wrap">
            <input type="text" id="r-frate" placeholder="60,000" oninput="formatPrice(this,'r-frate-disp')" inputmode="numeric">
            <span class="price-unit">원</span>
          </div>
          <div class="price-display" id="r-frate-disp"></div>
        </div>
      </div>
    </div>
    <div class="reg-section">
      <h3>🕐 레슨 가능 시간</h3>
      <div class="chip-label" style="margin-bottom:7px">요일</div>
      <div class="day-grid">
        <button class="day-btn" onclick="this.classList.toggle('on')">월</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">화</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">수</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">목</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">금</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">토</button>
        <button class="day-btn" onclick="this.classList.toggle('on')">일</button>
      </div>
      <div class="chip-label" style="margin-bottom:7px">시간대</div>
      <div class="time-grid">
        <button class="time-btn" onclick="this.classList.toggle('on')"><span class="t-name">🌅 아침</span><span class="t-range">06:00~09:00</span></button>
        <button class="time-btn" onclick="this.classList.toggle('on')"><span class="t-name">☀️ 오전</span><span class="t-range">09:00~12:00</span></button>
        <button class="time-btn" onclick="this.classList.toggle('on')"><span class="t-name">🌤️ 오후</span><span class="t-range">12:00~18:00</span></button>
        <button class="time-btn" onclick="this.classList.toggle('on')"><span class="t-name">🌙 저녁</span><span class="t-range">18:00~22:00</span></button>
      </div>
    </div>
    <div class="reg-section">
      <h3>📝 자기소개</h3>
      <div class="intro-hint">💡 예시: "10년 경력 KTA 1급 코치입니다. 초보자부터 대회 준비까지 맞춤 레슨을 제공합니다."</div>
      <textarea id="r-intro" rows="3" placeholder="간단한 자기소개 (100자 내외)" oninput="document.getElementById('intro-cnt').textContent=this.value.length+' / 100자'"></textarea>
      <span class="form-hint" id="intro-cnt">0 / 100자</span>
    </div>
    <div class="step-nav">
      <button class="btn-prev" onclick="goStep(4)">← 이전</button>
      <button class="btn-next" id="btn-submit" onclick="submitCoach()">✅ 등록 신청</button>
    </div>
  </div>

  <!-- 등록 완료 화면 -->
  <div id="step-done" class="step-panel">
    <div class="success-screen">
      <div class="success-icon">🎉</div>
      <div class="success-title">등록 신청 완료!</div>
      <div class="success-desc">코치님의 등록 신청을 받았습니다.<br>검토 후 인증 완료 시 플랫폼에 노출됩니다.<br><br>보통 <b>1~3일 이내</b> 검토가 완료됩니다.</div>
      <button class="btn-go-home" onclick="showPage('coaches');setNav(document.querySelectorAll('.bnav-btn')[0]);resetForm()">코치 목록 보러가기 →</button>
    </div>
  </div>

</div>

<!-- BOTTOM NAV -->
<div class="bottom-nav">
  <button class="bnav-btn active" onclick="showPage('coaches');setNav(this)"><span class="bnav-icon">🏠</span>홈</button>
  <button class="bnav-btn" onclick="showPage('coaches');setNav(this)"><span class="bnav-icon">🔍</span>검색</button>
  <button class="bnav-btn" onclick="showPage('register');setNav(this)"><span class="bnav-icon">➕</span>등록</button>
  <button class="bnav-btn" onclick="alert('로그인 후 이용 가능합니다')"><span class="bnav-icon">👤</span>프로필</button>
</div>

<!-- 코치 모달 -->
<div class="modal-bg" id="modal" onclick="if(event.target===this)this.classList.remove('show')">
  <div class="modal">
    <button class="close-btn" onclick="document.getElementById('modal').classList.remove('show')">✕</button>
    <div class="modal-handle"></div>
    <div id="mc"></div>
  </div>
</div>
<div class="toast" id="toast"></div>

<script>
const { createClient } = supabase;
const sb = createClient(
  'https://icawwicosjfbxtfxynto.supabase.co',
  'sb_publishable_R4kAGdEINHuwh2lvknvCog_BSMTBnmB'
);

const subregions={서울:['강남구','강동구','강북구','강서구','관악구','광진구','구로구','금천구','노원구','도봉구','동대문구','동작구','마포구','서대문구','서초구','성동구','성북구','송파구','양천구','영등포구','용산구','은평구','종로구','중구','중랑구'],경기도:['수원시','성남시','용인시','부천시','안산시','안양시','남양주시','화성시','평택시','의정부시','시흥시','파주시','광명시','김포시','광주시','군포시','하남시','오산시','양주시','이천시','구리시','안성시','포천시','의왕시','여주시','동두천시','과천시'],인천:['계양구','남동구','동구','미추홀구','부평구','서구','연수구','중구','강화군','옹진군'],강원도:['춘천시','원주시','강릉시','동해시','속초시','삼척시','태백시'],경상남도:['창원시','진주시','통영시','사천시','김해시','밀양시','거제시','양산시'],경상북도:['포항시','경주시','구미시','안동시','영주시','영천시'],충청남도:['천안시','아산시','서산시','논산시','공주시','보령시'],충청북도:['청주시','충주시','제천시'],전라남도:['목포시','여수시','순천시','나주시','광양시'],전라북도:['전주시','군산시','익산시','정읍시'],제주도:['제주시','서귀포시']};

let aLevel='',aSkill='',aLesson='',allCoaches=[],curSlide=0,sliderTimer,photoFileData=null,videoLinkCount=1,currentStep=1;

// ── 스텝 이동 ──
function goStep(n) {
  if(n===2){
    const name=document.getElementById('r-name').value.trim();
    const email=document.getElementById('r-email').value.trim();
    const region=document.getElementById('r-region').value;
    if(!name){showToast('이름을 입력해주세요','error');return;}
    if(!email||!email.includes('@')){showToast('올바른 이메일을 입력해주세요','error');return;}
    if(!region){showToast('광역시/도를 선택해주세요','error');return;}
  }
  document.getElementById('step'+currentStep).classList.remove('on');
  if(n>5){
    document.getElementById('step-done').classList.add('on');
  } else {
    document.getElementById('step'+n).classList.add('on');
  }
  // 진행 바
  for(let i=1;i<=5;i++){
    const dot=document.getElementById('pd'+i);
    dot.classList.remove('done','active');
    if(i<n) dot.classList.add('done');
    else if(i===n) dot.classList.add('active');
  }
  document.getElementById('prog-fill').style.width=(n/5*100)+'%';
  currentStep=n;
  window.scrollTo({top:0,behavior:'smooth'});
}

function resetForm(){
  currentStep=1;
  document.querySelectorAll('.step-panel').forEach(p=>p.classList.remove('on'));
  document.getElementById('step1').classList.add('on');
  document.getElementById('step-done').classList.remove('on');
  for(let i=1;i<=5;i++){const d=document.getElementById('pd'+i);d.classList.remove('done','active');if(i===1)d.classList.add('active');}
  document.getElementById('prog-fill').style.width='20%';
}

// ── 가격 포맷 ──
function formatPrice(input,dispId){
  const raw=input.value.replace(/[^0-9]/g,'');
  if(raw){input.value=parseInt(raw).toLocaleString();document.getElementById(dispId).textContent=parseInt(raw).toLocaleString()+'원';}
  else{input.value='';document.getElementById(dispId).textContent='';}
}
function getPriceVal(id){const v=document.getElementById(id).value.replace(/,/g,'');return v?parseInt(v):null;}

// ── 미디어 ──
function switchTab(btn,panelId){
  btn.parentElement.querySelectorAll('.media-tab').forEach(t=>t.classList.remove('on'));btn.classList.add('on');
  btn.closest('.form-row').querySelectorAll('.media-panel').forEach(p=>p.classList.remove('on'));
  document.getElementById(panelId).classList.add('on');
}
function previewPhotoUrl(url){
  const p=document.getElementById('photo-url-prev'),img=document.getElementById('photo-url-img');
  if(url){img.src=url;p.style.display='block';}else p.style.display='none';
}
function previewPhotoFile(input){
  const file=input.files[0];if(!file)return;
  const r=new FileReader();
  r.onload=e=>{photoFileData=e.target.result;document.getElementById('photo-file-img').src=e.target.result;document.getElementById('photo-file-prev').style.display='flex';};
  r.readAsDataURL(file);
}
function clearPhotoFile(){photoFileData=null;document.getElementById('r-photo-file').value='';document.getElementById('photo-file-prev').style.display='none';}
function addVideoLink(){
  if(videoLinkCount>=3){showToast('영상 링크는 최대 3개입니다','error');return;}
  videoLinkCount++;
  const div=document.createElement('div');div.className='link-row';
  div.innerHTML=`<input type="url" placeholder="https://youtube.com/... 또는 https://instagram.com/reel/..."><button class="link-remove-btn" onclick="this.parentElement.remove();videoLinkCount--">삭제</button>`;
  document.getElementById('video-links').appendChild(div);
}
function getVideoLinks(){return[...document.querySelectorAll('#video-links input')].map(i=>i.value.trim()).filter(Boolean);}
function getSelectedDays(){return[...document.querySelectorAll('.day-btn.on')].map(b=>b.textContent).join(',');}
function getSelectedTimes(){return[...document.querySelectorAll('.time-btn.on')].map(b=>b.querySelector('.t-name').textContent.trim()).join(',');}

// ── 코치 불러오기 ──
async function loadCoaches(){
  document.getElementById('coaches-list').innerHTML='<div class="loading">⏳ 코치 목록 불러오는 중...</div>';
  const{data,error}=await sb.from('coaches').select('*').order('rating',{ascending:false});
  if(error){document.getElementById('coaches-list').innerHTML='<div class="no-results">❌ 불러오기 실패: '+error.message+'</div>';return;}
  allCoaches=data||[];filterCoaches();
}

// ── 코치 등록 — 이메일 중복 체크 추가 ──
async function submitCoach(){
  const hOn=document.getElementById('opt-hitting').classList.contains('on');
  const fOn=document.getElementById('opt-feed').classList.contains('on');
  if(!hOn&&!fOn){showToast('레슨 종류를 하나 이상 선택해주세요','error');return;}
  if(!parseInt(document.getElementById('r-ccareer').value)){showToast('코칭 경력을 입력해주세요','error');return;}

  const email=document.getElementById('r-email').value.trim();
  const btn=document.getElementById('btn-submit');
  btn.disabled=true;btn.textContent='⏳ 확인 중...';

  // 이메일 중복 체크
  const{data:existing}=await sb.from('coaches').select('id').eq('email',email).maybeSingle();
  if(existing){
    showToast('이미 등록된 이메일입니다','error');
    btn.disabled=false;btn.textContent='✅ 등록 신청';return;
  }

  const days=getSelectedDays(),times=getSelectedTimes();
  const photoUrl=document.getElementById('r-photo-url').value.trim();
  const payload={
    name:document.getElementById('r-name').value.trim(),
    email,
    phone:document.getElementById('r-phone').value.trim(),
    region:document.getElementById('r-region').value,
    city:document.getElementById('r-sub').value,
    experience_coach:parseInt(document.getElementById('r-ccareer').value)||0,
    experience_player:parseInt(document.getElementById('r-pcareer').value)||0,
    coach_background:document.getElementById('r-cbg').value.trim(),
    player_background:document.getElementById('r-pbg').value.trim(),
    hitting_rate:hOn?getPriceVal('r-hrate'):null,
    feed_rate:fOn?getPriceVal('r-frate'):null,
    lessons:[hOn&&'hitting',fOn&&'feed'].filter(Boolean).join(','),
    specialties:[...document.querySelectorAll('#r-levels .chip.on')].map(c=>c.textContent).join(','),
    skills:[...document.querySelectorAll('#r-skills .chip.on')].map(c=>c.textContent).join(','),
    introduction:document.getElementById('r-intro').value.trim(),
    available_times:(days&&times)?days+' / '+times:(days||times),
    profile_image:photoUrl||photoFileData||null,
    video_links:getVideoLinks().join(','),
    verification_status:'pending',rating:0,review_count:0
  };

  btn.textContent='⏳ 저장 중...';
  const{error}=await sb.from('coaches').insert([payload]);
  btn.disabled=false;btn.textContent='✅ 등록 신청';

  if(error){showToast('❌ 저장 실패: '+error.message,'error');return;}

  // 완료 화면으로
  document.getElementById('step5').classList.remove('on');
  document.getElementById('step-done').classList.add('on');
  window.scrollTo({top:0,behavior:'smooth'});
  await loadCoaches();
}

// ── 필터 & 렌더링 ──
function filterCoaches(){
  const region=document.getElementById('f-region').value,sub=document.getElementById('f-sub').value,sort=document.getElementById('f-sort').value;
  let list=allCoaches.filter(c=>{
    const lv=(c.specialties||'').split(','),sk=(c.skills||'').split(','),le=(c.lessons||'').split(',');
    return(!region||c.region===region)&&(!sub||c.city===sub)&&(!aLevel||lv.includes(aLevel))&&(!aSkill||sk.includes(aSkill))&&(!aLesson||le.includes(aLesson));
  });
  if(sort==='rating')list.sort((a,b)=>(b.rating||0)-(a.rating||0));
  else if(sort==='exp')list.sort((a,b)=>(b.experience_coach||0)-(a.experience_coach||0));
  else if(sort==='price-low')list.sort((a,b)=>Math.min(a.hitting_rate||9e9,a.feed_rate||9e9)-Math.min(b.hitting_rate||9e9,b.feed_rate||9e9));
  else if(sort==='price-high')list.sort((a,b)=>Math.max(b.hitting_rate||0,b.feed_rate||0)-Math.max(a.hitting_rate||0,a.feed_rate||0));
  renderCoaches(list);
}

function renderCoaches(list){
  const el=document.getElementById('coaches-list');
  document.getElementById('cnt').textContent=list.length+'명';
  if(!list.length){el.innerHTML='<div class="no-results">조건에 맞는 코치가 없습니다</div>';return;}
  el.innerHTML=list.map(c=>{
    const le=(c.lessons||'').split(',').filter(Boolean),lv=(c.specialties||'').split(',').filter(Boolean),sk=(c.skills||'').split(',').filter(Boolean);
    const pills=le.map(l=>l==='hitting'?`<span class="lp lp-h">🔵 히팅</span>`:`<span class="lp lp-f">🟠 볼피딩</span>`).join('');
    const lvT=lv.slice(0,2).map(s=>`<span class="spec-tag">${s}</span>`).join('');
    const skT=sk.slice(0,2).map(s=>`<span class="skill-tag">${s}</span>`).join('');
    const exp=`코칭 ${c.experience_coach||0}년${c.experience_player>0?' · 선수 '+c.experience_player+'년':''}`;
    const av=c.profile_image?`<img src="${c.profile_image}" onerror="this.parentElement.innerHTML='${(c.name||'?')[0]}'">`:(c.name||'?')[0];
    let pr='';
    if(c.hitting_rate)pr+=`<div class="price-item"><span class="pdot pdot-h"></span><span class="pval">₩${c.hitting_rate.toLocaleString()}</span><span class="plbl"> 히팅</span></div>`;
    if(c.feed_rate)pr+=`<div class="price-item"><span class="pdot pdot-f"></span><span class="pval">₩${c.feed_rate.toLocaleString()}</span><span class="plbl"> 볼피딩</span></div>`;
    return`<div class="coach-card" onclick="openModal('${c.id}')">
      <div class="card-top"><div class="avatar">${av}</div>
        <div class="coach-info">
          <div class="coach-name-row"><span class="coach-name">${c.name}</span>${c.verification_status==='verified'?'<span class="verified-badge">✅ 인증</span>':''}</div>
          <div class="coach-loc">📍 ${c.region||''} ${c.city||''} · ${exp}</div>
          <div class="rating-row"><span class="stars">${'★'.repeat(Math.round(c.rating||0))}</span><span>${c.rating||0} (${c.review_count||0}개)</span></div>
          <div class="lesson-pills">${pills}</div>
        </div>
      </div>
      <div class="card-bottom"><div class="card-tags">${lvT}${skT}</div><div class="price-row">${pr}</div></div>
    </div>`;
  }).join('');
}

function openModal(id){
  const c=allCoaches.find(x=>x.id===id);if(!c)return;
  const le=(c.lessons||'').split(',').filter(Boolean),lv=(c.specialties||'').split(',').filter(Boolean),sk=(c.skills||'').split(',').filter(Boolean);
  const pills=le.map(l=>l==='hitting'?`<span class="lp lp-h" style="padding:4px 10px;font-size:12px">🔵 히팅 레슨</span>`:`<span class="lp lp-f" style="padding:4px 10px;font-size:12px">🟠 볼피딩 레슨</span>`).join('');
  let pr='<div class="mprice-box">';
  if(c.hitting_rate)pr+=`<div class="mprice-row"><span class="mprice-lbl"><span class="pdot pdot-h" style="width:8px;height:8px"></span>히팅 레슨</span><span class="mprice-val">₩${c.hitting_rate.toLocaleString()}<span style="font-size:11px;font-weight:400;color:var(--text3)">/시간</span></span></div>`;
  if(c.feed_rate)pr+=`<div class="mprice-row"><span class="mprice-lbl"><span class="pdot pdot-f" style="width:8px;height:8px"></span>볼피딩 레슨</span><span class="mprice-val">₩${c.feed_rate.toLocaleString()}<span style="font-size:11px;font-weight:400;color:var(--text3)">/시간</span></span></div>`;
  pr+='</div>';
  const av=c.profile_image?`<img src="${c.profile_image}" style="width:100%;height:100%;object-fit:cover">`:(c.name||'?')[0];
  const vids=(c.video_links||'').split(',').filter(Boolean);
  document.getElementById('mc').innerHTML=`
    <div class="modal-avatar">${av}</div>
    <div class="modal-name">${c.name} ${c.verification_status==='verified'?'✅':''}</div>
    <div class="modal-loc">📍 ${c.region||''} ${c.city||''}</div>
    <div class="modal-stats">
      <div class="mstat"><div class="n">${c.rating||0}</div><div class="l">평점</div></div>
      <div class="mstat"><div class="n">${c.experience_coach||0}년</div><div class="l">코칭경력</div></div>
      <div class="mstat"><div class="n">${c.experience_player>0?c.experience_player+'년':'—'}</div><div class="l">선수경력</div></div>
    </div>
    ${lv.length?`<div class="msec"><h4>전문 분야</h4><div class="tag-row">${lv.map(s=>`<span class="spec-tag" style="padding:4px 10px;font-size:12px">${s}</span>`).join('')}</div></div><div class="divider"></div>`:''}
    ${sk.length?`<div class="msec"><h4>전문 스킬</h4><div class="tag-row">${sk.map(s=>`<span class="skill-tag" style="padding:4px 10px;font-size:12px">${s}</span>`).join('')}</div></div><div class="divider"></div>`:''}
    <div class="msec"><h4>레슨 종류</h4><div class="tag-row">${pills}</div></div>
    <div class="divider"></div>
    <div class="msec"><h4>요금</h4>${pr}</div>
    ${c.available_times?`<div class="divider"></div><div class="msec"><h4>레슨 가능 시간</h4><p>${c.available_times}</p></div>`:''}
    ${c.player_background||c.coach_background?`<div class="divider"></div>${c.player_background?`<div class="msec"><h4>선수 경력</h4><p style="white-space:pre-line">${c.player_background}</p></div>`:''} ${c.coach_background?`<div class="msec"><h4>코칭 경력</h4><p style="white-space:pre-line">${c.coach_background}</p></div>`:''}`:''}
    ${c.introduction?`<div class="divider"></div><div class="msec"><h4>소개</h4><p style="line-height:1.7">${c.introduction}</p></div>`:''}
    ${vids.length?`<div class="divider"></div><div class="msec"><h4>코칭 영상</h4><div class="modal-video">${vids.map(v=>`<a href="${v}" target="_blank">▶ ${v.includes('instagram')?'인스타그램':'유튜브'} 영상 보기 →</a>`).join('')}</div></div>`:''}
    <button class="btn-book" onclick="alert('예약 기능은 곧 오픈됩니다! 🎾')">📅 레슨 예약하기</button>`;
  document.getElementById('modal').classList.add('show');
}

function tc(el,g,v){const m={level:'lf',skill:'sf',lesson:'lesf'};document.querySelectorAll('#'+m[g]+' .chip').forEach(c=>c.classList.remove('on'));el.classList.add('on');if(g==='level')aLevel=v;else if(g==='skill')aSkill=v;else aLesson=v;filterCoaches();}
function onRegionChange(){const r=document.getElementById('f-region').value,s=document.getElementById('f-sub');s.innerHTML='<option value="">전체</option>';if(r&&subregions[r]){s.disabled=false;subregions[r].forEach(v=>{const o=document.createElement('option');o.value=o.textContent=v;s.appendChild(o);});}else s.disabled=true;filterCoaches();}
function onRegRegionChange(){const r=document.getElementById('r-region').value,s=document.getElementById('r-sub');s.innerHTML='<option value="">선택</option>';if(r&&subregions[r]){s.disabled=false;subregions[r].forEach(v=>{const o=document.createElement('option');o.value=o.textContent=v;s.appendChild(o);});}else s.disabled=true;}
function showPage(p){document.getElementById('coaches-page').style.display=p==='coaches'?'block':'none';document.getElementById('register-page').className='reg-page'+(p==='register'?' show':'');if(p==='register')resetForm();}
function setNav(btn){document.querySelectorAll('.bnav-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');}
function setLang(l,btn){document.querySelectorAll('.lang-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');}
function showToast(msg,type='success'){const t=document.getElementById('toast');t.textContent=msg;t.className='toast show '+type;setTimeout(()=>t.className='toast',3000);}
function goSlide(n){curSlide=n;document.getElementById('bt').style.transform=`translateX(-${n*100}%)`;document.querySelectorAll('.banner-dot').forEach((d,i)=>d.classList.toggle('on',i===n));}
function startSlider(){sliderTimer=setInterval(()=>goSlide((curSlide+1)%4),3500);}
function stopSlider(){clearInterval(sliderTimer);}
document.querySelector('.banner-slider').addEventListener('mouseenter',stopSlider);
document.querySelector('.banner-slider').addEventListener('mouseleave',startSlider);
startSlider();
loadCoaches();
</script>
</body>
</html>
